import 'package:flutter/widgets.dart';

class SwipeBackNotification extends Notification {
  const SwipeBackNotification();
}

class ShowChatNotification extends Notification {
  const ShowChatNotification();
}
